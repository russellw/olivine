# Sort Python functions and classes
# Internal tool, designed for this project only
# Does NOT work for arbitrary Python code
import python

import etc

for file in java.src_files():
    v = etc.read_lines(file)
    r = java.compose(a)
    if r != v:
        print(file)
        etc.write_lines(file, r)
